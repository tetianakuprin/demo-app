import styled from "styled-components";

export const InputColumWrapper = styled.div``;

export const InputWrapper = styled.input`
  &:focus {
    outline: none;
    outline-offset: 0px;
  }
`;

export const InputWrapperRefresh = styled.input`
  &:focus {
    outline: none;
    outline-offset: 0px;
  }
`;
